# API de Generación de Convenios PDF

Ver documentación completa en el [README principal](../README.md)
```

4. Click en **"Commit new file"**

✅ **Listo, carpeta `api/` creada**

---

## 📦 PASO 2: Mover los archivos

Ahora vamos a mover cada archivo a su carpeta correcta.

### 2.1 Mover `index.html` → `formulario/`

1. En la página principal del repositorio, **click en `index.html`**
2. Arriba a la derecha, click en el **icono del lápiz** ✏️ (Edit this file)
3. **Arriba**, donde dice el nombre del archivo, verás: `Formulario_Convenio / index.html`
4. **Borra** `index.html` y escribe: `formulario/index.html`
5. Scroll hacia abajo
6. En "Commit changes", escribe: `Mover index.html a carpeta formulario`
7. Click en **"Commit changes"**

✅ **`index.html` movido a `formulario/`**

---

### 2.2 Mover `logo_avanta_principal.png` → `formulario/`

**Las imágenes no se pueden mover con el editor, hay que subirlas de nuevo:**

1. **Descarga** el logo a tu computadora:
   - Click en `logo_avanta_principal.png`
   - Click derecho en la imagen → **"Save image as..."**
   - Guárdalo

2. **Borra** el logo de la raíz:
   - Vuelve a la página principal del repo
   - Click en `logo_avanta_principal.png`
   - Click en el **icono de la basura** 🗑️ (arriba a la derecha)
   - Confirma: **"Commit changes"**

3. **Sube** el logo a la carpeta correcta:
   - Ve a la página principal del repo
   - **Click en la carpeta `formulario/`**
   - Click en **"Add file"** → **"Upload files"**
   - **Arrastra** el logo que descargaste
   - Commit: `Agregar logo a carpeta formulario`
   - Click en **"Commit changes"**

✅ **Logo movido a `formulario/`**

---

### 2.3 Mover `generar-convenio.js` → `api/`

1. Vuelve a la página principal del repo
2. **Click en `generar-convenio.js`**
3. Click en el **lápiz** ✏️ (Edit)
4. Arriba, cambia el nombre de `generar-convenio.js` a `api/generar-convenio.js`
5. Scroll hacia abajo
6. Commit: `Mover generar-convenio.js a carpeta api`
7. Click en **"Commit changes"**

✅ **`generar-convenio.js` movido a `api/`**

---

### 2.4 Mover `package.json` → `api/`

1. Vuelve a la página principal
2. **Click en `package.json`**
3. Click en el **lápiz** ✏️
4. Cambia el nombre a `api/package.json`
5. Commit: `Mover package.json a carpeta api`
6. Click en **"Commit changes"**

✅ **`package.json` movido a `api/`**

---

### 2.5 Mover `workflow_convenio.json` → `n8n/`

1. Vuelve a la página principal
2. **Click en `workflow_convenio.json`**
3. Click en el **lápiz** ✏️
4. Cambia el nombre a `n8n/workflow_convenio.json`
5. Commit: `Mover workflow_convenio.json a carpeta n8n`
6. Click en **"Commit changes"**

✅ **`workflow_convenio.json` movido a `n8n/`**

---

## ✅ PASO 3: Verificar que todo está correcto

Después de todos estos pasos, en la página principal de tu repositorio deberías ver:
```
📄 CAMPOS_CONVENIO.md
📄 COMO_SUBIR_A_GITHUB.md
📄 Convenio_Avanta_Hotel.pdf
📄 GUIA_COMPLETA.md
📄 README.md
📁 api/
📁 formulario/
📁 n8n/
